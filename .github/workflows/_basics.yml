name: Basic Python Workflow

on:
  workflow_call:
    inputs:
      black-options:
        description: "Options to pass to the Black formatter"
        required: false
        type: string
        default: ""

jobs:
  call-another-reusable:
    uses: .github/workflows/black.yml@main
    with:
      options: "${{ inputs.black-options }}"
